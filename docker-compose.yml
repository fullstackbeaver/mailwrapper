services:
  mailWrapper:
    build: .
    restart: unless-stopped
    ports:
      - "8025:8025"
    env_file:
      - .env
    networks:
      beaverflow:
        aliases:
          - mailWrapper

networks:
  beaverflow:
    external: true